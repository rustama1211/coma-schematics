{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicsKnexSeedMake",
  "title": "KnexMake Options Schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of seed.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name you like to use for the seed?"
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path to create the module."
    },
    "moduleName": {
      "type": "string",
      "description": "The module name to create migration"
    },
    "module": {
      "type": "string",
      "format": "path",
      "description": "The path to import the module."
    },
    "language": {
      "type": "string",
      "description": "Nest module language (ts/js)."
    },
    "sourceRoot": {
      "type": "string",
      "description": "Nest module source root directory."
    },
    "skipImport": {
      "type": "boolean",
      "description": "Flag to skip the module import.",
      "default": false
    },
    "crud": {
      "type": "boolean",
      "description": "When true, CRUD entry points are generated.",
      "default": true
    },
    "flat": {
      "type": "boolean",
      "default": false,
      "description": "Flag to indicate if a directory is created."
    }
  },
  "required": ["name"]
}
